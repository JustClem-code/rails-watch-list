<%@lists.each do |list|%>
  <%= link_to list_path(list), class: "text-decoration-none" do %>
    <div class="card">
      <div class="card-body">
        <%= list.name%>
      </div>
    </div>
  <% end %>
<%end%>
<div>
  <%= link_to "New", new_list_path, class: "btn btn-primary mt-3"%>
</div>
